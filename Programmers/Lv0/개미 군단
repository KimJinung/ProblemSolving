class Solution {
    public int solution(int hp) {
        int answer = 0;

        for (int power : new int[]{5, 3, 1}) {
            answer += hp / power;
            hp %= power;
        }

        return answer;
    }
}