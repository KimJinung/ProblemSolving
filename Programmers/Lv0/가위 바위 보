import java.util.*;
import java.util.stream.*;


class Solution {
    public String solution(String rsp) {
        return Arrays.stream(rsp.split(""))
                .map(ch -> ch.equals("2") ? "0" : ch.equals("0") ? "5" : "2")
                .collect(Collectors.joining());
    }
}